@model IEnumerable<RainbowWine.Data.DeliveryAgentLogin>

@{
    ViewBag.Title = "LoginDetails";
}

<h2>LoginDetails</h2>



<p>
    @Html.ActionLink("Back to List", "List")
</p>
<table class="table table-striped table-bordered" id="listtable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OnDuty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OffDuty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryAgent.DeliveryExecName)
            </th>
           
        </tr>


    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OnDuty)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OffDuty)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DeliveryAgent.DeliveryExecName)
                </td>
                @*<td>
                        @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                    </td>*@
            </tr>
        }

    </tbody>
</table>
